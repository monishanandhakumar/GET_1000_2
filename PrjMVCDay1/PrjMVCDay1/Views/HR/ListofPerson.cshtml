@model IEnumerable<PrjMVCDay1.Models.Person>
@{
    ViewData["Title"] = "ListofPerson";
}

<h1>ListofPerson</h1>

@foreach(var item in Model)
            {
                 <li>@item.name</li>
                 <li>@item.city</li>
            }